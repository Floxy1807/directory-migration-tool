# MoveWithSymlink WPF 版本说明

## ✅ 成功！单文件 EXE 已完成

本项目现在提供了 **WPF 版本**的 GUI 应用，完美支持**单文件 EXE** 打包！

## 📦 项目结构

```
moveFloder/
├── MigrationCore/              # 核心迁移逻辑库（共享）
├── MoveWithSymlinkWPF/         # ✨ WPF GUI 应用（新）
│   ├── ViewModels/
│   ├── Converters/
│   └── Properties/
├── MoveWithSymlinkGUI/         # WinUI 3 GUI（多文件）
├── MoveWithSymlink.ps1         # PowerShell 脚本版本
└── publish-wpf.ps1             # WPF 发布脚本
```

## 🚀 快速开始

### 方式 1：使用发布脚本（推荐）

```powershell
# 一键发布为单文件 EXE
.\publish-wpf.ps1
```

### 方式 2：手动发布

```powershell
# 发布为单文件 EXE
dotnet publish MoveWithSymlinkWPF\MoveWithSymlinkWPF.csproj -p:PublishProfile=win-x64 -c Release

# 运行
.\MoveWithSymlinkWPF\bin\publish\win-x64\MoveWithSymlinkWPF.exe
```

## 📊 版本对比

| 特性 | WPF 版本 ✅ | WinUI 3 版本 | PowerShell 脚本 |
|------|------------|-------------|----------------|
| 单文件 EXE | ✅ 支持 | ❌ 不支持 | ✅ 单个 .ps1 |
| 文件大小 | ~75 MB | ~65 MB (多文件) | <1 MB |
| 无需安装 .NET | ✅ | ✅ | ❌ 需要 PowerShell |
| 现代化 UI | ✅ | ✅✅ | ❌ 命令行 |
| 功能完整性 | ✅ 100% | ✅ 100% | ✅ 100% |
| Windows 版本 | Win 10/11 | Win 10/11 | Win 7+ |

## 📁 输出文件

发布后会生成：
- `MoveWithSymlinkWPF.exe` - 主程序（单文件，约 75MB）
- `MoveWithSymlinkWPF.pdb` - 调试符号（可选，可删除）
- `MigrationCore.pdb` - 调试符号（可选，可删除）

**分发时只需要 `MoveWithSymlinkWPF.exe` 这一个文件！**

## 🎯 功能特性

### 核心功能（与其他版本完全相同）
- ✅ 扫描并分析源目录文件
- ✅ 智能识别大文件
- ✅ 使用 Robocopy 高效复制
- ✅ 自动创建符号链接
- ✅ 完整的错误处理和回滚机制
- ✅ 实时进度显示和日志

### WPF 特有优势
- ✅ **真正的单文件 EXE**
- ✅ 更小的依赖
- ✅ 更好的兼容性
- ✅ 经典稳定的 WPF 框架

## 🎨 界面截图

### 步骤 1：选择路径
- 浏览选择源目录和目标目录
- 配置大文件阈值和线程数
- 自动验证路径有效性

### 步骤 2：扫描分析
- 显示总文件数、大小
- 显示大文件统计
- 检查磁盘空间

### 步骤 3：执行迁移
- 6 阶段进度显示
- 实时日志输出
- 支持取消操作

### 步骤 4：完成
- 显示迁移结果
- 成功或失败状态

## 💡 使用建议

### 推荐使用场景
1. **需要分发给其他用户** → 使用 WPF 单文件 EXE
2. **自己使用** → PowerShell 脚本最简单
3. **需要最现代的 UI** → WinUI 3（多文件）

### 系统要求
- Windows 10 版本 1809 (Build 17763) 或更高
- Windows 11（推荐）
- x64 架构

### 权限要求
- 管理员权限，或
- 启用 Windows 开发者模式

## 🔧 开发说明

### 构建项目

```powershell
# 构建所有项目
dotnet build MoveWithSymlink.sln -c Release

# 只构建 WPF 项目
dotnet build MoveWithSymlinkWPF\MoveWithSymlinkWPF.csproj -c Release
```

### 项目依赖
- .NET 8.0 SDK
- CommunityToolkit.Mvvm (8.2.2)
- MigrationCore（项目引用）

## 📝 技术细节

### 为什么 WPF 支持单文件而 WinUI 3 不支持？

**WinUI 3 限制：**
- 需要 `resources.pri` 资源文件必须在 EXE 旁边
- Windows App Runtime 有额外的文件依赖
- 微软官方文档说明不建议单文件发布

**WPF 优势：**
- 成熟稳定的框架（2006 年发布）
- 完美支持 .NET 单文件发布
- 所有资源可以嵌入 EXE

### 核心技术栈
- **UI 框架**: WPF (Windows Presentation Foundation)
- **MVVM 框架**: CommunityToolkit.Mvvm
- **核心逻辑**: MigrationCore（共享库）
- **文件操作**: Robocopy + 符号链接

## 🎉 总结

现在您有了三个版本可以选择：

1. **WPF GUI (推荐)** - 单文件 EXE，易于分发 ✨
2. **WinUI 3 GUI** - 最现代的 UI，多文件
3. **PowerShell 脚本** - 最简单，适合技术用户

所有版本共享相同的 `MigrationCore` 核心库，功能完全一致！

---

**祝您使用愉快！** 🚀

